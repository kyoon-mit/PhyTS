# Foundation-model benchmark dependencies (Python 3.10).
#
# MOMENT + Chronos run in the project's main Python 3.12 environment.
# The packages below are needed only for the Python 3.10 wrappers:
# TimesFM, Time-MoE, MOIRAI, and Lag-Llama.
#
# Install:
#   conda create -p <env_prefix> python=3.10 -y
#   <env_prefix>/bin/pip install -r benchmarks/foundation/requirements_fm.txt
#   <env_prefix>/bin/pip install 'git+https://github.com/time-series-foundation-models/lag-llama.git#egg=lag-llama'
#   <env_prefix>/bin/pip install 'setuptools<72'   # restores pkg_resources needed by lag-llama
#
# Lag-Llama checkpoint:
#   huggingface-cli download time-series-foundation-models/Lag-Llama lag-llama.ckpt \
#       --local-dir <your_checkpoints_dir>/lag-llama/

torch==2.4.1
numpy==1.26.4
pandas==2.1.4
einops==0.7.0
h5py
wandb
pyyaml
jsonargparse[all]
matplotlib

# Foundation models
timesfm==1.3.0
transformers==4.44.2
accelerate==1.13.0
uni2ts==2.0.0
