# wandb sweep config — MLP end-to-end classification
#
# Create sweep:
#   wandb sweep benchmarks/TESS/sweep_configs/mlp.yaml --project TimeSeriesPhysics
#
# Then launch agents via:
#   bash benchmarks/TESS/run_sweep.sh --model_type mlp --sweep_id <id>

program: benchmarks/TESS/sweep_classification.py
command:
  - ${env}
  - python
  - ${program}
  - --model_type=mlp
  - ${args}

method: bayes
metric:
  name: val/acc
  goal: maximize

early_terminate:
  type: hyperband
  min_iter: 10    # minimum epochs before a run can be pruned
  eta: 3          # bracket factor: keep 1/eta of runs each round

parameters:
  size:
    values: [xs, sm, md, lg]
  lr:
    distribution: log_uniform_values
    min: 0.00005
    max: 0.01
  dropout:
    distribution: uniform
    min: 0.0
    max: 0.5
  weight_decay:
    distribution: log_uniform_values
    min: 0.000001
    max: 0.01
  batch_size:
    values: [32, 64, 128]
  seed:
    values: [0, 1, 2]
