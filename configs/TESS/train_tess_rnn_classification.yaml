# Usage: python main.py fit --config configs/TESS/train_tess_rnn_classification.yaml

trainer:
  max_epochs: 100
  accelerator: gpu
  devices: 1
  log_every_n_steps: 1
  callbacks:
    - class_path: lightning.pytorch.callbacks.EarlyStopping
      init_args:
        monitor: val/loss
        mode: min
        patience: 15
    - class_path: lightning.pytorch.callbacks.ModelCheckpoint
      init_args:
        dirpath: checkpoints/tess_rnn_classification
        filename: best
        monitor: val/loss
        mode: min
        save_top_k: 1
  logger:
    class_path: lightning.pytorch.loggers.CSVLogger
    init_args:
      save_dir: logs
      name: tess_rnn_classification

model:
  class_path: tasks.TESS.classification.TESSClassification
  init_args:
    lr: 5.0e-4
    lr_decay: 0.995
    weight_decay: 1.0e-4
    use_mask: true
    model:
      class_path: models.rnn.RNNClassifier
      init_args:
        d_input: 1
        d_output: 8
        d_model: 128
        n_layers: 3
        dropout: 0.2

data:
  class_path: dataloader.tess_dataloader.TESSDataModule
  init_args:
    data_path: data/TESS/tess_classification.parquet
    seq_len: 3917
    batch_size: 64
    num_workers: 4
    train_frac: 0.7
    val_frac: 0.15
    seed: 42
