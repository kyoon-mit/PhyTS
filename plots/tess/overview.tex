\begin{table}[t]
\centering
\small
\caption{%
  Foundation-model benchmark on the PhyTS-bench TESS subset.
  \emph{Classification} predicts one of 8 stellar-variability classes
  (\textsc{aperiodic}, \textsc{contact\_rot}, \textsc{dsct\_bcep},
  \textsc{eclipse}, \textsc{gdor\_spb}, \textsc{instrument/junk},
  \textsc{rrlyr\_ceph}, \textsc{solarlike}) and reports test-set accuracy.
  \emph{Regression} predicts the rotation frequency $f_{\mathrm{rot}}$
  (cycles per day) and reports test-set $R^2$.
  Both tasks use the same TIC-grouped train/val/test split (seed 42)
  so no star appears in more than one split.
  Light curves are padded or truncated to 1024 samples.
  Three regimes are compared: zero-shot linear probe on frozen
  embeddings, LoRA fine-tuning ($r{=}8$, $\alpha{=}16$, head LR $10^{-3}$,
  backbone LR $10^{-4}$), and full fine-tuning of all backbone parameters
  (backbone LR $10^{-5}$). Best run per model is reported, selected by
  validation accuracy (classification) or validation MSE (regression).
  All numbers are single-seed (seed~42); no across-seed standard deviation
  was collected. \textemdash{} indicates a run not yet completed; LoRA on
  Granite-TTM is unsupported by the wrapper and is therefore omitted.
  Lag-Llama is omitted (single inference pass exceeds the available compute
  budget).
  }
  \label{tab:tess_benchmark}
  \begin{tabular}{lccc@{\hskip 1.5em}ccc}
    \toprule
    & \multicolumn{3}{c}{Classification: Accuracy $\uparrow$} & \multicolumn{3}{c}{Regression: $R^2$ $\uparrow$} \\
    \cmidrule(lr){2-4} \cmidrule(lr){5-7}
    Model & Zero-shot & LoRA & Full FT & Zero-shot & LoRA & Full FT \\
    \midrule
    MOMENT & 0.8349 & 0.8642 & 0.8739 & 0.2631 & 0.3801 & 0.3749 \\
    Chronos & 0.8197 & 0.8346 & 0.8417 & 0.3049 & 0.2749 & 0.3174 \\
    TimesFM & 0.8239 & 0.8783 & 0.7985 & 0.0740 & 0.4095 & 0.2735 \\
    Time-MoE & 0.7909 & 0.8789 & 0.8888 & 0.1715 & 0.3056 & 0.3098 \\
    MOIRAI & 0.8095 & 0.8760 & 0.8815 & 0.2366 & 0.3718 & 0.3804 \\
    Granite-TTM & 0.6868 & \textemdash & 0.8085 & 0.2050 & \textemdash & 0.2742 \\
    \bottomrule
  \end{tabular}
\end{table}
